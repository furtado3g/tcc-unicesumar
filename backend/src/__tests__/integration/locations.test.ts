import app from "../../configs/serverTest"
import supertest from "supertest"
const request = supertest(app)
describe('Teste do gereciamento de Locais',()=>{
    it('Criar novo tipo de local',async done => {
        
    })
    it('',async done => {

    })
})